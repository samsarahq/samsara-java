/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.samsara.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.samsara.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AgentStudioVoiceSessionTranscriptEntryResponseBody.Builder.class)
public final class AgentStudioVoiceSessionTranscriptEntryResponseBody {
    private final long endMilliseconds;

    private final AgentStudioVoiceSessionTranscriptEntryResponseBodySpeakerType speakerType;

    private final long startMilliseconds;

    private final String text;

    private final Map<String, Object> additionalProperties;

    private AgentStudioVoiceSessionTranscriptEntryResponseBody(
            long endMilliseconds,
            AgentStudioVoiceSessionTranscriptEntryResponseBodySpeakerType speakerType,
            long startMilliseconds,
            String text,
            Map<String, Object> additionalProperties) {
        this.endMilliseconds = endMilliseconds;
        this.speakerType = speakerType;
        this.startMilliseconds = startMilliseconds;
        this.text = text;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Offset from the start of the session at which this utterance ended, in milliseconds. Approximated from the start of the next utterance (or the end of the session for the final utterance).
     */
    @JsonProperty("endMilliseconds")
    public long getEndMilliseconds() {
        return endMilliseconds;
    }

    /**
     * @return Who produced this utterance. Defaults to <code>unknown</code> when the speaker cannot be confidently classified.  Valid values: <code>agent</code>, <code>driver</code>, <code>admin</code>, <code>unknown</code>
     */
    @JsonProperty("speakerType")
    public AgentStudioVoiceSessionTranscriptEntryResponseBodySpeakerType getSpeakerType() {
        return speakerType;
    }

    /**
     * @return Offset from the start of the session at which this utterance began, in milliseconds.
     */
    @JsonProperty("startMilliseconds")
    public long getStartMilliseconds() {
        return startMilliseconds;
    }

    /**
     * @return Transcribed text of the utterance.
     */
    @JsonProperty("text")
    public String getText() {
        return text;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AgentStudioVoiceSessionTranscriptEntryResponseBody
                && equalTo((AgentStudioVoiceSessionTranscriptEntryResponseBody) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AgentStudioVoiceSessionTranscriptEntryResponseBody other) {
        return endMilliseconds == other.endMilliseconds
                && speakerType.equals(other.speakerType)
                && startMilliseconds == other.startMilliseconds
                && text.equals(other.text);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.endMilliseconds, this.speakerType, this.startMilliseconds, this.text);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static EndMillisecondsStage builder() {
        return new Builder();
    }

    public interface EndMillisecondsStage {
        /**
         * <p>Offset from the start of the session at which this utterance ended, in milliseconds. Approximated from the start of the next utterance (or the end of the session for the final utterance).</p>
         */
        SpeakerTypeStage endMilliseconds(long endMilliseconds);

        Builder from(AgentStudioVoiceSessionTranscriptEntryResponseBody other);
    }

    public interface SpeakerTypeStage {
        /**
         * <p>Who produced this utterance. Defaults to <code>unknown</code> when the speaker cannot be confidently classified.  Valid values: <code>agent</code>, <code>driver</code>, <code>admin</code>, <code>unknown</code></p>
         */
        StartMillisecondsStage speakerType(
                @NotNull AgentStudioVoiceSessionTranscriptEntryResponseBodySpeakerType speakerType);
    }

    public interface StartMillisecondsStage {
        /**
         * <p>Offset from the start of the session at which this utterance began, in milliseconds.</p>
         */
        TextStage startMilliseconds(long startMilliseconds);
    }

    public interface TextStage {
        /**
         * <p>Transcribed text of the utterance.</p>
         */
        _FinalStage text(@NotNull String text);
    }

    public interface _FinalStage {
        AgentStudioVoiceSessionTranscriptEntryResponseBody build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements EndMillisecondsStage, SpeakerTypeStage, StartMillisecondsStage, TextStage, _FinalStage {
        private long endMilliseconds;

        private AgentStudioVoiceSessionTranscriptEntryResponseBodySpeakerType speakerType;

        private long startMilliseconds;

        private String text;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AgentStudioVoiceSessionTranscriptEntryResponseBody other) {
            endMilliseconds(other.getEndMilliseconds());
            speakerType(other.getSpeakerType());
            startMilliseconds(other.getStartMilliseconds());
            text(other.getText());
            return this;
        }

        /**
         * <p>Offset from the start of the session at which this utterance ended, in milliseconds. Approximated from the start of the next utterance (or the end of the session for the final utterance).</p>
         * <p>Offset from the start of the session at which this utterance ended, in milliseconds. Approximated from the start of the next utterance (or the end of the session for the final utterance).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("endMilliseconds")
        public SpeakerTypeStage endMilliseconds(long endMilliseconds) {
            this.endMilliseconds = endMilliseconds;
            return this;
        }

        /**
         * <p>Who produced this utterance. Defaults to <code>unknown</code> when the speaker cannot be confidently classified.  Valid values: <code>agent</code>, <code>driver</code>, <code>admin</code>, <code>unknown</code></p>
         * <p>Who produced this utterance. Defaults to <code>unknown</code> when the speaker cannot be confidently classified.  Valid values: <code>agent</code>, <code>driver</code>, <code>admin</code>, <code>unknown</code></p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("speakerType")
        public StartMillisecondsStage speakerType(
                @NotNull AgentStudioVoiceSessionTranscriptEntryResponseBodySpeakerType speakerType) {
            this.speakerType = Objects.requireNonNull(speakerType, "speakerType must not be null");
            return this;
        }

        /**
         * <p>Offset from the start of the session at which this utterance began, in milliseconds.</p>
         * <p>Offset from the start of the session at which this utterance began, in milliseconds.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("startMilliseconds")
        public TextStage startMilliseconds(long startMilliseconds) {
            this.startMilliseconds = startMilliseconds;
            return this;
        }

        /**
         * <p>Transcribed text of the utterance.</p>
         * <p>Transcribed text of the utterance.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("text")
        public _FinalStage text(@NotNull String text) {
            this.text = Objects.requireNonNull(text, "text must not be null");
            return this;
        }

        @java.lang.Override
        public AgentStudioVoiceSessionTranscriptEntryResponseBody build() {
            return new AgentStudioVoiceSessionTranscriptEntryResponseBody(
                    endMilliseconds, speakerType, startMilliseconds, text, additionalProperties);
        }
    }
}
