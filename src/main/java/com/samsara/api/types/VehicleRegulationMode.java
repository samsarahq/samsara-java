/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.samsara.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class VehicleRegulationMode {
    public static final VehicleRegulationMode REGULATED = new VehicleRegulationMode(Value.REGULATED, "regulated");

    public static final VehicleRegulationMode UNREGULATED = new VehicleRegulationMode(Value.UNREGULATED, "unregulated");

    public static final VehicleRegulationMode MIXED = new VehicleRegulationMode(Value.MIXED, "mixed");

    private final Value value;

    private final String string;

    VehicleRegulationMode(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof VehicleRegulationMode
                        && this.string.equals(((VehicleRegulationMode) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case REGULATED:
                return visitor.visitRegulated();
            case UNREGULATED:
                return visitor.visitUnregulated();
            case MIXED:
                return visitor.visitMixed();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static VehicleRegulationMode valueOf(String value) {
        switch (value) {
            case "regulated":
                return REGULATED;
            case "unregulated":
                return UNREGULATED;
            case "mixed":
                return MIXED;
            default:
                return new VehicleRegulationMode(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        REGULATED,

        UNREGULATED,

        MIXED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitRegulated();

        T visitUnregulated();

        T visitMixed();

        T visitUnknown(String unknownType);
    }
}
